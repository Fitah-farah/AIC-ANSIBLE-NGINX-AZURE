# Step 1: Use the official lightweight Nginx image
FROM nginx:alpine

# Step 2: Copy your HTML file into Nginx's default static directory
COPY index.html /usr/share/nginx/html/

# Step 3: Expose port 80 to the outside world
EXPOSE 80

# Step 4: Nginx will start automatically using the default command
